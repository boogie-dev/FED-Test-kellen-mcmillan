const switchAndPrint = (item) => {
  const ele = item.title
  const node = document.createElement('li')
  const anchor = document.createElement('a')
  switch (ele.length !== 0) {
    case ele[0] === 'A': {
      const listParent = document.getElementById('contentA')
      const alphabetLetter = document.getElementById('a')
      listParent.classList.remove('hidden')
      alphabetLetter.classList.remove('greyedOut')
      node.innerText = ele
      anchor.setAttribute('href', '#')
      anchor.setAttribute('class', 'nostyle')
      listParent.appendChild(anchor)
      anchor.appendChild(node)
      break
    }
    case ele[0] === 'B': {
      const listParent = document.getElementById('contentB')
      const alphabetLetter = document.getElementById('b')
      listParent.classList.remove('hidden')
      alphabetLetter.classList.remove('greyedOut')
      node.innerText = ele
      anchor.setAttribute('href', '#')
      anchor.setAttribute('class', 'nostyle')
      listParent.appendChild(anchor)
      anchor.appendChild(node)
      break
    }
    case ele[0] === 'C': {
      const listParent = document.getElementById('contentC')
      const alphabetLetter = document.getElementById('c')
      listParent.classList.remove('hidden')
      alphabetLetter.classList.remove('greyedOut')
      node.innerText = ele
      anchor.setAttribute('href', '#')
      anchor.setAttribute('class', 'nostyle')
      listParent.appendChild(anchor)
      anchor.appendChild(node)
      break
    }
    case ele[0] === 'D': {
      const listParent = document.getElementById('contentD')
      const alphabetLetter = document.getElementById('d')
      listParent.classList.remove('hidden')
      alphabetLetter.classList.remove('greyedOut')
      node.innerText = ele
      anchor.setAttribute('href', '#')
      anchor.setAttribute('class', 'nostyle')
      listParent.appendChild(anchor)
      anchor.appendChild(node)
      break
    }
    case ele[0] === 'E': {
      const listParent = document.getElementById('contentE')
      const alphabetLetter = document.getElementById('e')
      listParent.classList.remove('hidden')
      alphabetLetter.classList.remove('greyedOut')
      node.innerText = ele
      anchor.setAttribute('href', '#')
      anchor.setAttribute('class', 'nostyle')
      listParent.appendChild(anchor)
      anchor.appendChild(node)
      break
    }
    case ele[0] === 'F': {
      const listParent = document.getElementById('contentF')
      const alphabetLetter = document.getElementById('f')
      listParent.classList.remove('hidden')
      alphabetLetter.classList.remove('greyedOut')
      node.innerText = ele
      anchor.setAttribute('href', '#')
      anchor.setAttribute('class', 'nostyle')
      listParent.appendChild(anchor)
      anchor.appendChild(node)
      break
    }
    case ele[0] === 'G': {
      const listParent = document.getElementById('contentG')
      const alphabetLetter = document.getElementById('g')
      listParent.classList.remove('hidden')
      alphabetLetter.classList.remove('greyedOut')
      node.innerText = ele
      anchor.setAttribute('href', '#')
      anchor.setAttribute('class', 'nostyle')
      listParent.appendChild(anchor)
      anchor.appendChild(node)
      break
    }
    case ele[0] === 'H': {
      const listParent = document.getElementById('contentH')
      const alphabetLetter = document.getElementById('h')
      listParent.classList.remove('hidden')
      alphabetLetter.classList.remove('greyedOut')
      node.innerText = ele
      anchor.setAttribute('href', '#')
      anchor.setAttribute('class', 'nostyle')
      listParent.appendChild(anchor)
      anchor.appendChild(node)
      break
    }
    case ele[0] === 'I': {
      const listParent = document.getElementById('contentI')
      const alphabetLetter = document.getElementById('i')
      listParent.classList.remove('hidden')
      alphabetLetter.classList.remove('greyedOut')
      node.innerText = ele
      anchor.setAttribute('href', '#')
      anchor.setAttribute('class', 'nostyle')
      listParent.appendChild(anchor)
      anchor.appendChild(node)
      break
    }
    case ele[0] === 'J': {
      const listParent = document.getElementById('contentJ')
      const alphabetLetter = document.getElementById('j')
      listParent.classList.remove('hidden')
      alphabetLetter.classList.remove('greyedOut')
      node.innerText = ele
      anchor.setAttribute('href', '#')
      anchor.setAttribute('class', 'nostyle')
      listParent.appendChild(anchor)
      anchor.appendChild(node)
      break
    }
    case ele[0] === 'K': {
      const listParent = document.getElementById('contentK')
      const alphabetLetter = document.getElementById('k')
      listParent.classList.remove('hidden')
      alphabetLetter.classList.remove('greyedOut')
      node.innerText = ele
      anchor.setAttribute('href', '#')
      anchor.setAttribute('class', 'nostyle')
      listParent.appendChild(anchor)
      anchor.appendChild(node)
      break
    }
    case ele[0] === 'L': {
      const listParent = document.getElementById('contentL')
      const alphabetLetter = document.getElementById('l')
      listParent.classList.remove('hidden')
      alphabetLetter.classList.remove('greyedOut')
      node.innerText = ele
      anchor.setAttribute('href', '#')
      anchor.setAttribute('class', 'nostyle')
      listParent.appendChild(anchor)
      anchor.appendChild(node)
      break
    }
    case ele[0] === 'M': {
      const listParent = document.getElementById('contentM')
      const alphabetLetter = document.getElementById('m')
      listParent.classList.remove('hidden')
      alphabetLetter.classList.remove('greyedOut')
      node.innerText = ele
      anchor.setAttribute('href', '#')
      anchor.setAttribute('class', 'nostyle')
      listParent.appendChild(anchor)
      anchor.appendChild(node)
      break
    }
    case ele[0] === 'N': {
      const listParent = document.getElementById('contentN')
      const alphabetLetter = document.getElementById('n')
      listParent.classList.remove('hidden')
      alphabetLetter.classList.remove('greyedOut')
      node.innerText = ele
      anchor.setAttribute('href', '#')
      anchor.setAttribute('class', 'nostyle')
      listParent.appendChild(anchor)
      anchor.appendChild(node)
      break
    }
    case ele[0] === 'O': {
      const listParent = document.getElementById('contentO')
      const alphabetLetter = document.getElementById('o')
      listParent.classList.remove('hidden')
      alphabetLetter.classList.remove('greyedOut')
      node.innerText = ele
      anchor.setAttribute('href', '#')
      anchor.setAttribute('class', 'nostyle')
      listParent.appendChild(anchor)
      anchor.appendChild(node)
      break
    }
    case ele[0] === 'P': {
      const listParent = document.getElementById('contentP')
      const alphabetLetter = document.getElementById('p')
      listParent.classList.remove('hidden')
      alphabetLetter.classList.remove('greyedOut')
      node.innerText = ele
      anchor.setAttribute('href', '#')
      anchor.setAttribute('class', 'nostyle')
      listParent.appendChild(anchor)
      anchor.appendChild(node)
      break
    }
    case ele[0] === 'Q': {
      const listParent = document.getElementById('contentQ')
      const alphabetLetter = document.getElementById('q')
      listParent.classList.remove('hidden')
      alphabetLetter.classList.remove('greyedOut')
      node.innerText = ele
      anchor.setAttribute('href', '#')
      anchor.setAttribute('class', 'nostyle')
      listParent.appendChild(anchor)
      anchor.appendChild(node)
      break
    }
    case ele[0] === 'R': {
      const listParent = document.getElementById('contentR')
      const alphabetLetter = document.getElementById('r')
      listParent.classList.remove('hidden')
      alphabetLetter.classList.remove('greyedOut')
      node.innerText = ele
      anchor.setAttribute('href', '#')
      anchor.setAttribute('class', 'nostyle')
      listParent.appendChild(anchor)
      anchor.appendChild(node)
      break
    }
    case ele[0] === 'S': {
      const listParent = document.getElementById('contentS')
      const alphabetLetter = document.getElementById('s')
      listParent.classList.remove('hidden')
      alphabetLetter.classList.remove('greyedOut')
      node.innerText = ele
      anchor.setAttribute('href', '#')
      anchor.setAttribute('class', 'nostyle')
      listParent.appendChild(anchor)
      anchor.appendChild(node)
      break
    }
    case ele[0] === 'T': {
      const listParent = document.getElementById('contentT')
      const alphabetLetter = document.getElementById('t')
      listParent.classList.remove('hidden')
      alphabetLetter.classList.remove('greyedOut')
      node.innerText = ele
      anchor.setAttribute('href', '#')
      anchor.setAttribute('class', 'nostyle')
      listParent.appendChild(anchor)
      anchor.appendChild(node)
      break
    }
    case ele[0] === 'U': {
      const listParent = document.getElementById('contentU')
      const alphabetLetter = document.getElementById('u')
      listParent.classList.remove('hidden')
      alphabetLetter.classList.remove('greyedOut')
      node.innerText = ele
      anchor.setAttribute('href', '#')
      anchor.setAttribute('class', 'nostyle')
      listParent.appendChild(anchor)
      anchor.appendChild(node)
      break
    }
    case ele[0] === 'V': {
      const listParent = document.getElementById('contentV')
      const alphabetLetter = document.getElementById('v')
      listParent.classList.remove('hidden')
      alphabetLetter.classList.remove('greyedOut')
      node.innerText = ele
      anchor.setAttribute('href', '#')
      anchor.setAttribute('class', 'nostyle')
      listParent.appendChild(anchor)
      anchor.appendChild(node)
      break
    }
    case ele[0] === 'W': {
      const listParent = document.getElementById('contentW')
      const alphabetLetter = document.getElementById('w')
      listParent.classList.remove('hidden')
      alphabetLetter.classList.remove('greyedOut')
      node.innerText = ele
      anchor.setAttribute('href', '#')
      anchor.setAttribute('class', 'nostyle')
      listParent.appendChild(anchor)
      anchor.appendChild(node)
      break
    }
    case ele[0] === 'X': {
      const listParent = document.getElementById('contentX')
      const alphabetLetter = document.getElementById('x')
      listParent.classList.remove('hidden')
      alphabetLetter.classList.remove('greyedOut')
      node.innerText = ele
      anchor.setAttribute('href', '#')
      anchor.setAttribute('class', 'nostyle')
      listParent.appendChild(anchor)
      anchor.appendChild(node)
      break
    }
    case ele[0] === 'Y': {
      const listParent = document.getElementById('contentY')
      const alphabetLetter = document.getElementById('y')
      listParent.classList.remove('hidden')
      alphabetLetter.classList.remove('greyedOut')
      node.innerText = ele
      anchor.setAttribute('href', '#')
      anchor.setAttribute('class', 'nostyle')
      listParent.appendChild(anchor)
      anchor.appendChild(node)
      break
    }
    case ele[0] === 'Z': {
      const listParent = document.getElementById('contentZ')
      const alphabetLetter = document.getElementById('z')
      listParent.classList.remove('hidden')
      alphabetLetter.classList.remove('greyedOut')
      node.innerText = ele
      anchor.setAttribute('href', '#')
      anchor.setAttribute('class', 'nostyle')
      listParent.appendChild(anchor)
      anchor.appendChild(node)
      break
    }
    default: {
      return null
    }
  }
  return true
}
fetch('./assets/data.json')
  .then((response) => {
    return response.json()
  })
  .then((data) => {
    Object.fromEntries(
      Object.entries(data.termList).map((list) => {
        console.log('My items ', list[1])
        return switchAndPrint(list[1])
      })
    )
    return true
  })
